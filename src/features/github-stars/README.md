# Feature: `github-stars`

Widget que muestra el conteo de estrellas del repo público en GitHub. Usado en Footer y sección hero.

## Estructura

```
features/github-stars/
└── components/
    ├── GitHubStarsDisplay.tsx
    └── index.ts
```

## Pendiente

Este es un widget muy pequeño — podría migrar a `shared/components/` si no crece más. Por ahora lo dejamos como feature por consistencia con la arquitectura.
